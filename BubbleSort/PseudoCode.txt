bubblesort(unsortedarray)
	if (unsortedarray.length == 1 or unsortedarray.length == 0)
		return unsortedarray

	for (i from N-1 to 0)
		for (j from 1 to i)
			if (unsortedarray[j-1] > unsortedarray[i])
				swap (unsortedarray[j-1], unsortedarray[j])
